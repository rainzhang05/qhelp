name: Test Queue

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-queue:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: swift build -c release
      - name: Run RequestQueueTests
        run: swift run -c release qhelpTests RequestQueueTests
